const SearchForm = () => {
  return (
    <form>
      <input
        type="text"
        className="form-control form-control-lg"
        placeholder="Book title..."
      />
    </form>
  );
};

export default SearchForm;
